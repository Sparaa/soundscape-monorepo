This repository is Soundscape, a self-hosted AI radio that runs entirely in Docker (Windows: Docker Desktop + WSL 2).
Read `AGENTS.md` at the repo root before suggesting any command: it has the exact install procedure with checks,
a diagnosis playbook, and rules (nothing is installed on the host; never change the version pins; never
`docker compose down -v`).
